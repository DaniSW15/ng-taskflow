// ¡No pongas secretos aquí! Solo la URL pública de la API.
// La seguridad depende del backend y la autenticación, no de ocultar la URL.

export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080/api'
};
